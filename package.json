{
  "name": "tripboard",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
